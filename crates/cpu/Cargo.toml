[package]
name = "gejmboj_cpu"
version = "0.1.0"
edition = "2024"

[lints]
workspace = true

[dependencies]
log = { workspace = true }

[dev-dependencies]
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0.150"
